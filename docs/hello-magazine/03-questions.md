# 03. 疑問点・要確認事項

スライド読み取りと実装の過程で出てきた疑問点。**🔴=回答待ちで方針が固まらない / 🟡=設計に影響 / 🟢=後回し可**。

---

## 🔴 ブロッカー（先に方針を決めたい）

### Q0. Google Slides のコメント（スレッド）が取得できていない
- 今回はスライド**本文テキスト**から要件を読み取った。API制約でスレッド型コメント
  （吹き出し）は取得不可だった（`commentsNotSupported`）。
- スライド上に文章として書かれた「確認事項」「コメント風メモ」は反映済み。
- **依頼**: 吹き出しコメントに重要な指示がある場合、テキストで共有してほしい
  （またはコメントをスライド本文へ転記いただけると確実）。

### Q1. 多言語対応 ✅方針・方式とも確定
- **手動翻訳 × Polylang** で確定（ユーザー決定）。Polylang 3.8.5 導入、5言語設定済み、
  CPT/タクソノミーを翻訳対象にコード化（`inc/polylang.php`）。→ [05-translation.md](05-translation.md)。
- 残作業: 静的テキスト（テーマ内文言）の各言語登録、言語スイッチャのテンプレ実装。
- ※ 口コミ等 UGC の「人力>自動翻訳」ロジックはアプリ層の別課題（マガジン外）。

### Q2. CMSのタグ管理体系 ✅対応済み
- CMSタグ管理スライド（`1p-AO2dz...`）を読み取り、**表記統一(20260608)・タグマスタ**を
  [04-master-lists.md](04-master-lists.md) に整理。
- `hello_persona / hello_curriculum / hello_region / hello_price / hello_tag` に
  **初期タームを投入済み**（`inc/seed-terms.php`）。
- 残課題: マガジンの「タグを押すと種別が表示」TOPフィルタUIの実装（テンプレート側）。

### Q3. 「学校(school)」データ基盤 ※当面は手動運用で確定
- ユーザー回答：**まずは手動**で進める。
- よってランキング／エージェントの学校情報は **ACF 手動エントリ**で運用（実装済み）。
- 将来、学校検索・口コミ基盤と連携して自動化する場合は school CPT ＋クエリ連携を追加
  （Q4 と連動）。サイト構成（`magazine.` サブドメインと本体の役割分担）は将来整理。

---

## 🟡 設計に影響（実装と並行で詰めたい）

### Q4. ランキングの学校カード「自動挿入」
- 要望: フロント検索の結果をランキングに食べログ風に自動表示。
- 現状: **手動エントリ（ACF繰り返し）で実装可能**な形にしてある。
- 自動化する場合は school CPT ＋ クエリ連携の追加開発が必要（Q3に依存）。予算判断を確認したい。
  （スライドにも「予算的に無理なら手動で大丈夫」とあり、当面は手動運用想定）

### Q5. YouTube LIVE の「秒数サムネ＋その秒から再生」
- 「タップしたら該当秒から再生」は**実装済み**（チャプター/Q&Aクリックで `start` 秒から再生）。
- 「その秒数の画面をサムネイルみたいに出したい」は**未対応（方式要決定）**。
  YouTubeが任意秒のサムネを公式提供しないため：
  - (a) チャプターごとに**サムネ画像を手動登録**（運用増だが確実）← ACFに画像枠を足せば対応
  - (b) 動画から自動でフレーム抽出（追加実装・コスト大）
- **確認**: (a)/(b) どちらで進めるか。

### Q6. FAQ の区分（①〜④）の持ち方
- 回答者（保護者/卒業生）× 対象（保護者向け/生徒向け）× 時期（入学検討中/在学中）の軸がある。
- 現状: `faq_audience` セレクト（4区分）＋ `hello_persona` で表現。
- **確認**: これをタクソノミーとして絞り込み・URL化したいか（一覧ページの設計に影響）。

### Q7. 「コピペで記事量産」をどう支援するか
- 「猪口さんがコピペで様々な記事展開」を実現したい。
- 候補: ① 投稿複製機能（"複製"ボタン）② ブロックパターン/再利用ブロック登録
  ③ 各CPTの雛形（下書きテンプレ）。
- **確認**: 希望する運用イメージ（どれが一番ラクか）。実装方針を決めたい。

### Q8. ランキングの★評価の出所
- ★評価（学習環境/学校生活/保護者対応/費用満足度/総合）は、
  口コミ集計か手動入力か。スライドに「各校5件以上の口コミが集まり次第公開」とある。
- 現状: **手動入力**前提（ACF数値）。口コミ連動は Q3/Q4 の基盤次第。

---

## 🟢 記録（将来要件・合意事項）

### Q9. CPT をテーマに同梱したこと
- ご指示どおり CPT/ACF を**子テーマ `hello` に同梱**した。
- 一般にCPTはテーマ非依存にするためプラグイン化が定石（テーマ変更でCPTが消えるのを防ぐ）。
- 当面はテーマ運用で問題ないが、**将来プラグイン化**も選択肢として記録。

### Q10. エージェント×口コミの「口コミ＋返信」（将来）
- 学校ページに口コミを紐づけ、口コミとエージェント返信をGoogle口コミ風に。
- 初期は猪口さん手動登録でOK（スライド明記）。データモデル（口コミCPT/コメント）は将来設計。

### Q11. エージェントの体制タグ・サポート項目の正式リスト
- ★合計20の配分や体制タグの**正式な項目セット**が確定すれば、
  ACFのチェックボックス選択肢を確定値に置き換える（現状はスライド例を初期値に設定）。

---

## 補足：現時点の実装スタンス
- 不明点は「**手動運用でも回る形**」を初期実装にしている（自動化は基盤確定後）。
- ACFフィールドは**初期スキャフォルド**。Q5〜Q11の回答で項目を精緻化する。
